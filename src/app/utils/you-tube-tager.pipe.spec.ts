import { YouTubeTagerPipe } from './you-tube-tager.pipe';

describe('YouTubeTagerPipe', () => {
  it('create an instance', () => {
    const pipe = new YouTubeTagerPipe();
    expect(pipe).toBeTruthy();
  });
});
